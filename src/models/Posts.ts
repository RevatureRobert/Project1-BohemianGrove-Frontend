export interface Posts {
    username: string;
    handle: string;
    timestamp: string;
    img: string;
    body: string;

    // constructor(username: string, handle: string, timestamp: string, img: string, body: string) {
    //     this.username = username;
    //     this.handle = handle;
    //     this.timestamp = timestamp;
    //     this.img = img;
    //     this.body = body;
    // }
}